package model1;

import org.openqa.selenium.WebDriver;

public class BaseModel {
WebDriver driver;
WebDriver pass;
WebDriver moba;
WebDriver month;
public BaseModel(WebDriver c, WebDriver d,WebDriver z,WebDriver q) {
	driver =c;
	pass = d;
	moba = z;
	month =q;
}
}
